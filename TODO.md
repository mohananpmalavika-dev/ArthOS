# TODO — Visual Design System (Restricted Palette)

- [x] Remove disallowed hard-coded colors from src/components/TrajectoryHeroVisual.jsx (use only #050713, #0B1220, cyan, purple, emerald, white)
- [ ] Remove disallowed inline styles in src/components/FlowNavigation.jsx (use only allowed palette; prefer CSS variables)
- [ ] Verify remaining color literals in the two component files are restricted to allowed palette.

- [ ] Build/test (run lint/build if available) to ensure no regressions.

